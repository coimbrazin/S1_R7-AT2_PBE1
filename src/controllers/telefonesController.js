const { telefonesModel } = require("../models/pedidosModel");

const telefonesController = {
    selecionarTodosTelefones: async (req, res) => {
        try {
            const { id_cliente } = req.query;
            let resultado;

            if (id_cliente) {
                resultado
            }
        } catch (error) {

        }
    }
}